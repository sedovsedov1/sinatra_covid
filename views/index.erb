<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

	<h1>Рязанский политехнический колледж</h1>
	<h2>Дистанцинное обучение</h2>

	<% if @enter == 1%>
		
		<h3>Преподаватель: <%= @login %></h3>
		<h3>Выберите вашу группу</h3>
		<% @file.each_line do |line| %>
		<a href='/prepods/<%= @login %>/<%= line %>'><%= line %></a> | 
		<% end %>

	<% else %>
		
		<form action="/" method="post">
			<table>
				<caption>Вход преподавателя или <a href="/admin">админа</a></caption>	
				<tr>
					<td>Логин</td>
					<td><input type="text" name="login"></td>
				</tr>
				<tr>
					<td>Пароль</td>
					<td><input type="password" name="password"></td>
				</tr>
				<tr>
					<td><input type="submit"></td>
				</tr>
			</table>
		</form>

		<h3>Выберите вашу группу</h3>
		<% @file.each_line do |line| %>
			<a href='/groups/<%= line %>'><%= line %></a> | 
		<% end %>

	<% end %>

</body>
</html>